#!/usr/bin/env bash

base_prog=$(basename "$(realpath -s "$0")")
base_dir="$(dirname "$(realpath -s "$0")")"
cmd="$@"

[[ ! -e "$base_dir/../../.git" ]] && base_dir="/usr/libexec/sodalite/bash"

echo $base_dir
echo $cmd
